[package]
name = "copy-fixture"
edition.workspace = true
license.workspace = true
version.workspace = true
publish = false

[dependencies]
ratatui.workspace = true
ratcn.workspace = true

# A copied component module is the whole point and nothing calls it: every item
# in every generated target is dead code here.
[lints.rust]
dead_code = "allow"
