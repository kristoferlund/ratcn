{
  "name": "ratcn-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "npm run docs:check && npm run demo:build && vitepress dev docs",
    "docs:build": "npm run docs:check && npm run demo:build && vitepress build docs",
    "docs:check": "node scripts/check-doc-regions.mjs",
    "demo:build": "scripts/build-demos.sh",
    "demo:dev": "scripts/demo-dev.sh"
  },
  "devDependencies": {
    "vitepress": "2.0.0-alpha.17"
  }
}
